import 'package:flutter/material.dart';

class ManageRestaurantInfo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Manage Menu'),
      ),
    );
  }
}
